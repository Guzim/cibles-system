/*
URI : <base_path>/target/<univers-name
Display a grid
Display the "ajustement"
Capacity to click in a area <=ajustement
Display the distance ? Non
Display the result of the zone.
*/

function targetPage(req, res) {
  /*
   check the name
   get the target yaml
  */
}
